# DeepFish · 项目总结

> 一份"如果只读一份文档，就读这份"的总结。
> 配套：`README.md`（怎么跑起来、怎么自检）。
> 写这份时的状态：六套测试 **1225 项断言全绿**，Stockfish 19 + deepseek-chat，能跑、能讲、能用。

---

## 一、这是什么

一个**给自己用的国际象棋复盘工具**：把棋谱贴进去，它能一页页翻、算棋、画曲线，
并且用中文**把"这一步发生了什么、为什么"讲出来**。

- 算棋：Stockfish 19（本地 exe）。
- 讲棋：DeepSeek（`deepseek-chat`，走本机后端转发，密钥不出这台机器）。
- 界面：一个页面，原生 JS，没有框架、没有构建步骤。

**它不是**：不是要替代棋软的引擎分析，也不是"AI 自动下棋"。
它想解决的是一个很窄的问题 —— **业余棋手看得懂引擎给的分数，但看不懂分数背后的道理**。

---

## 二、唯一的核心原则：事实和讲述分家

整个项目只有一条真正的原创想法，其他所有代码都是在给这条想法打杂：

> 🐟 **鳕鱼和代码负责「是什么」** —— 该走哪步、前后几分、谁在盯谁、这一步改变了什么。
> 🤖 **DeepSeek 只负责「为什么」** —— 把它讲成人话。
> 单子里**所有的数字、格子关系、前后变化**，要么是引擎输出、要么是代码按规则复算的；
> **模型一个字都不许自己产生。**

落到代码上就是三件事：

1. **讲棋走"订单"**：前端只负责把原始材料（FEN、引擎结果）送出去，
   后端用 chess.js **重新复算一遍**再拼成一张"事实卡"（`src/coach.js`）。
   前端算错了、或者接口被别处调用，讲出来的事实也不会跟着错。
2. **能算的都算好再给它**：谁悬着、这一步补了谁的保护、哪条线上钉着谁、
   两步之后能吃什么…… 全在 `src/position.js` 里用纯函数算完（可复算、幂等）。
3. **说不出来就承认**：材料里没有的，一律不许编；写"这一点我看不出来"是**被鼓励**的。

**这条原则救过很多次场。** 举三个真实例子（棋谱见下面那张"样例说明"）：

| 现象（哪盘棋 · 哪一步） | 查下去发现 |
|---|---|
| **样例一 · 第 9 手 9.Qd2**：模型说"d2 的后和 f4 的象一起盯住了 d6" | 后根本看不到 d6（d4 上挡着自己的兵）。根因是**这一步是安静着法，我们一个字都没喂** —— 它只能自己推格子关系 |
| **样例一 · 第 9 手 9.Qd2 之后那条引擎线**：模型把线里的**第 5 手**（`…Ne4`）说成"眼下的威胁" | 我们给了 PV，却从没告诉它"这是假想线，只有第 1 手是现在能走的" |
| **样例二 · 第 10 手 10.O-O**：模型说"给 g1 的王补上了保护" | 材料那行的原话是"**王 g1** 能护住自己的子：兵 f2…" —— 主语在最前面，读快了就成"王被护住" |

三次都不是"模型笨"，而是**材料缺了、或者材料的话能被读反**。

---

## 二之二、这份总结里反复提到的三盘样例

后面所有例子都只说"样例一 / 样例二 / 样例三 + 第几步"，对照表在这里
（三盘都进了界面，就是右上那三个按钮；按钮上只写编号，来源看这张表或鼠标悬停）：

|  | 样例一 | 样例二 | 样例三 |
|---|---|---|---|
| 界面按钮 | **样例一** | **样例二** | **样例三** |
| 棋谱文件 | `public/samples/sample1.pgn` | `public/samples/sample2.pgn` | `public/samples/sample3.pgn` |
| 来源 | lichess 一盘真实对局 | 国象联盟一盘真实对局 | lichess 一盘 rapid（`8XfgZTLp`） |
| 长度 | 51 步（26 白 + 25 黑） | 31 步（16 白 + 15 黑） | 102 步（51 + 51） |
| 特点 | 每步都带 `[%eval]` 评分、`[%clk]` 剩余时间、20 多处变着 | 没有评注；短兵相接；最后一步是**升变成马将杀** | 后 20 步几乎是纯王兵残局；第 50 手是"王吃不了旁边被保护的兵"的典型 |
| 总结里用到它哪几步 | **9.Qd2**（安静着法、d6 幻觉、假想线读错、念段名）、**19…Kd8**（"这步想干什么"讲不出、f7 被弃） | **10.O-O**（取舍 vs 漏着、f2 的假保护、王守谁读反）、**16.b8=N#**（升变，吃子栏最容易算错的地方） | **50.Kh5**（模型凭空说"白王能 Kxh6"，而 h6 被 g7 守着 —— 残局里最典型的一种编） |

---

## 三、它是怎么长出来的

27 轮迭代，大致八个阶段：

| 期 | 做了什么 | 标志 |
|---|---|---|
| ① 地基 | 棋谱解析（自己写的，chess.js 顶不住 lichess 的双评注）、棋子身份时间线、棋盘动画、引擎接入、评估曲线 | 前 8 轮 |
| ② 讲棋链路 | 接入 DeepSeek，定下"订单式提示词" | 第三轮 |
| ③ 提示词加厚 | 多路线候选、逐步局面、"走之前"那一层、逐子规则速查、起止格锚点、严格接地 | 第九～十二轮 |
| ④ 走进界面 | 直接在棋盘上走棋 → 记成**支线**（可悔棋、可导出成标准 PGN 变着） | 第十三轮 |
| ⑤ 事实层加厚 | 战场一览 → 去向表 → 线关系（牵制/穿刺）→ 保护变化 → 两步威胁 | 第十六～二十一轮 |
| ⑥ 措辞与分寸 | 别念材料、方向别读反、**没分量的话不说**、讲不出就留白 | 第二十～二十三轮 |
| ⑦ 界面细节 | 吃子栏、按钮位置、提示撤不掉 | 第二十四～二十五轮 |
| ⑧ 补最后两个窟窿 | 样例一/二/三都进界面（按钮只写编号）；**"够得着却吃不了"也写出来**（`blockedCaptures`） | 第二十六～二十七轮 |

看得出来：**前四期在造管道，后四期几乎全在治"模型讲错话"** ——
而每次查下去，绝大多数根因都在材料那边。

---

## 四、架构

```
浏览器 (public/)
  ├─ app.js        主逻辑：翻页、走棋、支线、缓存、记忆
  ├─ board.js      棋盘与棋子（只改属性，不重画）
  ├─ timeline.js   棋子身份时间线 + 吃子账
  ├─ pgn.js        棋谱解析（自己写的）
  ├─ eval.js       分数归一化与展示
  └─ chess.js      chess.js 的副本（必须和后端那份一模一样，有测试盯着）
        │
        │  fetch /api/analyze（局面 → 引擎）
        │  fetch /api/explain（这一步的全部事实 → 讲棋）
        ▼
后端 (src/)
  ├─ server.js     静态文件 + 四个接口；所有输入都过一遍筛子
  ├─ engine.js     鳕鱼驱动：UCI 握手、并发队列、评分翻转成"白方视角"
  ├─ coach.js      ★ 把事实拼成"订单" + 系统提示词
  ├─ position.js   ★ 事实层：全部是纯函数，可复算
  ├─ knowledge/    战术母题、开局原则、经典对局（只有沾边才附给模型）
  └─ llm.js        DeepSeek 调用：密钥从 .env 现读、错误分类、重试退避
```

**事实层（`src/position.js`）到底算了什么** —— 这是这个项目最值钱的文件：

| 类 | 函数 | 回答的问题 |
|---|---|---|
| 这一手 | `moveFacts` / `sanTrail` / `moveAnchor` | 谁从哪里走到哪里、吃了什么、带不带将、有几个合法着法 |
| 局面 | `positionProfile` / `materialOf` / `samePosition` | 局面特征、子力、前后是否接得上 |
| 威胁 | `immediateThreats` / `foeThreats` / `loosePieces` | 一步将杀、白吃、谁悬着（含"吃了能不能吃回"） |
| 一个子 | `pieceMap` / `attacksFrom` / `blockedCaptures` | 它吃得到谁、护得住谁、去得了哪；**够得着却吃不了**的，连"被谁挡着"一起写出来 |
| 线 | `firstPieceBeyond` / `lineTactics` | 谁挡在谁前面（牵制 / 穿刺） |
| 这一步的意图 | `protectionDelta` / `guardsOf` / `blockerBetween` | 它给谁补了保护、让谁没人守、原来是被谁挡住的 |
| 两步之后 | `followUp` / `captureMenu` | 顺着引擎那条线走两步，然后谁能吃什么 |
| 评价 | `choiceVerdict` / `engineEffect` / `mateReason` | 这一步是唯一解还是有好几条路、它挡掉了什么 |

**所有函数都是纯的**：给同样的输入，永远给同样的输出，不需要引擎、不联网。
所以它们能被 1225 项断言逐个钉住。

---

## 五、提示词这一块（最花时间的地方）

一张"事实卡"大致长这样（每段都是算出来的）：

```
【局面（按时间顺序）】      再往前一手 / 走之前 / 现在，三个 FEN
【这一步实际发生的事】      谁从哪到哪、吃了什么、带不带将（chess.js 重放）
【这一步动过的那个子能去哪】 吃得到谁 / 护着谁 / 能落到哪些空格 —— 表外就是到不了
【这一步给谁补了保护】      前后对比，★ 标出来
【战场一览】                谁能一步得手、谁悬着
【线关系】                  谁挡在谁前面
【引擎怎么看走之前的局面】   评分 / 对照（他走的 vs 引擎建议）/ 候选
【另一条路会改变谁的保护】   拿来和他实际走的对比
【引擎怎么看现在的局面】     最佳着法 + 变化线（每步带 [序号] 和 FEN）
【顺着引擎那条线走两步】     对方几个合法着法、两步之后轮到谁能吃什么
【这盘棋之前几手讲过什么】   多轮记忆（只有"要点"那一句）
【战术参考】                通用知识，只有沾边才附
```

配套的数字（`node probe-prompt.js --brief` 实测）：**系统提示词 5921 字符（≈2700 token）**，
送出去的"单子" 7110 字符，**一次讲棋总共约 1.3 万字符 ≈ 5900 token**
（prompt 缓存能命中系统提示词那一段）。

**系统提示词有 70 条断言钉着它** —— 删掉哪句会被测试当场抓住。
这也是为什么"剪提示词"这件事是安全的：它是一项有回归测试的重构，不是凭感觉删。

---

## 五之二、密钥是怎么关在这台机器里的

讲棋要调 DeepSeek，就得有一把 API 密钥。密钥漏出去的代价是**别人拿它刷你的账单**，
而从被提交到被爬虫扫走，窗口期是以分钟计的。所以这件事不能靠"小心"，得靠结构。

现在的链路只有一种可能：

```
浏览器 (public/)  ──►  本机后端 (src/server.js)  ──HTTPS──►  api.deepseek.com
                              ↑ 密钥只在这里、只在内存里
```

**五道闸门，从外往内：**

| # | 闸门 | 在哪 | 挡住什么 |
|---|---|---|---|
| 1 | 前端根本拿不到 | `public/` 里搜不到任何真密钥，也没有一行直连 DeepSeek 的代码 | F12 看源码、直接扒静态文件都白搭 —— 浏览器只跟 `localhost` 说话，只请求 `/api/*` |
| 2 | 密钥不进版本库 | `.gitignore` 挡掉 `.env` / `.env.local` / `.env.*.local` | 传到 GitHub、打包发给人都带不走它 |
| 3 | 出站只走请求头 | `llm.js` 里那一行 `Authorization: Bearer` | 密钥不进 URL/query，也就不会留在任何代理或网关的访问日志里 |
| 4 | 报错回显先擦一遍 | `scrubSecrets()` | 自建中转/反向代理在 4xx 里原样回吐请求头时，`sk-xxx` 会被擦成 `sk-***` |
| 5 | 给外面的自我介绍里压根没这个字段 | `info()` / `maskKey()` / `toJSON()` | `/api/llm`、`/api/health`、报错体 —— 三个出口只给"配没配""多长""打码预览" |

第 4、5 条是**刻意**的，不是恰好没写。`info()` 上就留着一行注释：
"这里**故意**没有 apiKey 字段 —— 这个对象会被整个塞进 HTTP 响应，多一个字段就是一次泄漏。"

**这些是实测过的，不是照代码注释抄的：**

- 把真密钥读进内存，去搜服务器六个 HTTP 出口的**原始响应体** ——
  `/api/health`、`/api/llm`、`/api/engine`、`/api/explain`（dryRun / 坏参数 / 空 body），
  **六个全部搜不到**（完整串和尾部指纹都搜不到）。
- 另有一个 `verify-key-safety.js`（16 项断言）：拿假密钥模拟"反向代理把 Authorization 头
  原样回显在 401 里"这种最坏情况，验证擦除和打码都拦住了。**0 失败。**

密钥的存放位置只有一个：**项目根目录的 `.env`**。部署到服务器上时也可以不用 `.env`，
直接把真·环境变量喂进去 —— `llm.js` 区分了这两种来源（文件没改过，就不覆盖已有的环境变量）。

> 想知道完整的前端行为（比如"前端到底会不会直连 DeepSeek"），
> 一句话就能验：`grep -rniE "apikey|sk-|deepseek\.com" public/` ——
> 命中的只有一段提示用户去填 `.env` 的**界面文案**，没有任何真东西。

---

## 六、踩过的坑

括号里标的是**这个坑是在哪盘棋、哪一步上冒出来的** ——
所以这些坑不只是"曾经发生过"，它们现在都还是 `verify-llm.js` / `verify-dom.js` 里的一条断言。

| # | 坑（哪盘棋 · 哪一步） | 教训 |
|---|---|---|
| 1 | 讲错了却只看日志（日志里只有"成功没、几毫秒"）—— 通用，第一次就栽在这 | **先把真正发出去的提示词原样打出来**（`probe-prompt.js` 的 dryRun）。判断"是没喂，还是喂错了"，是两条完全不同的修法 |
| 2 | **样例一 · 9.Qd2**：危险局面（有杀、有悬子）喂得很足，这一步**安静着法一个字没有** | "没有东西可说" ≠ "什么都不用给"。危险局面自己会喊，安静局面才是幻觉高发区 |
| 3 | 记谱只写 `Nc4`，两个马都"像"能去 —— 构造出来的最小例子 | 凡是进提示词的着法，一律带上**起止格锚点**（`马 b2→c4`），不让模型猜 |
| 4 | **样例一 · 9.Qd2 之后那条引擎线**：模型把线里的第 5 手（`…Ne4`）当成眼下的威胁 | 给一串变化线时，必须同时给它**时间和"这是假想线"的说明** |
| 5 | **样例二 · 10.O-O**：同一个局面两次真跑，模型说法不一样 | **先怀疑输入不稳定**。后来发现是引擎在两个同分着法之间翻首选 —— 模型两次都是照着材料说的 |
| 6 | **样例二 · 10.O-O**：材料里写"现在 d8 的王也守上了"，模型读成"给王补了保护" | **句子的主语必须显形**；给模型指路的措辞和给读者看的措辞要分开写 |
| 6b | **样例三 · 50.Kh5**：材料只写了"能落到的空格"，紧挨着王的 h6 兵一字没提；模型就补了一句"白王就能 Kxh6 吃掉 h6 的兵"（而那个兵被 g7 守着，规则上根本吃不了） | **"看着能做、材料沉默"是最危险的一种空白**：凡是它够得着的对方子，要么写"吃得到"，要么写"吃不了 + 谁守着"，不能不说 |
| 7 | **样例一 · 9.Qd2**：材料里印着`【线关系】`这样的段名，模型就照着念"代价立刻写在【线关系】里" | 我们怎么给材料分段，是**给我们自己看的**；正文里一个字都不该出现 |
| 8 | **样例二 · 10.O-O**：一个子本来有两个子守着，再加一个守着，材料还要报"补了一层保护" | **没分量的话不要说**。宁愿不说，也不要凑字 —— 这正是用户说的"乱讲" |
| 9 | **样例二 · 10.O-O**：两条路只差 0.05 个兵，材料却写"他没做另一条路做的事" | 材料的措辞会决定模型的**态度**。分数一样时只能说"取舍"，不能说"漏着" |
| 10 | 注释里的中文进了 `.bat`，cmd 直接解析失败 —— 与棋无关，是工程边界 | 工程边界上的事（编码、换行）也要写进文档 —— `start.bat` 现在必须纯 ASCII + CRLF |

一句话概括这十条：**模型的每一句错话，几乎都能在材料里找到一个"是我让它这么理解的"入口。**
而且注意这十坑里**有五个集中在样例二的同一步棋（10.O-O）上** ——
恰恰因为那是一步"引擎给分一样、但真实想法很深"的棋：材料越是讲不清，模型越容易替你编。
样例三的 50.Kh5 是同一个道理的**残局版**：那一步没什么深意（就是走错了），
可它旁边的 h6 兵"看着能吃"而我们没说"吃不了"，模型照样替你吃了一口。

---

## 七、它做得到什么、做不到什么

**做得到**（这是它的设计射程）：

- 一手棋里有**明显机制**的：漏子、杀棋、牵制、这一步给谁补了保护、哪条线通了。
  因为它手上有事实卡，讲解基本上就是"把卡念成人话"。
- 顺着引擎的变化线讲"接下来大概会怎样"，并说清哪一步是现在能走的、哪一步是假设。
- 老实说"这里我看不出来"。

**做不到**（而且不是改提示词能解决的）：

- **多步战术组合**（叉子、闪击、连续将军的次序）。我们只算一两步内的确定结论，
  三步以上的组合全靠引擎的 PV —— 而 PV 是**结论**，不是**道理**，模型只能复述，讲不出因果。
  （样例一里 `9.Qd2` 之后那条线就是例子：`…Ne4` 是第 5 手，而且是个弃子，讲不清就只能照着念。）
- **相持局面的计划**（兵形、弱格、该往哪边打）。这需要棋理知识库 + 可量化的判据，
  而"这个局面该走哪边"在不同对局里完全是两回事，量化不了。
- **价值语感的尺度**："明显优势"和"能接受"的分界只能靠引擎分数，和棋迷的语感不是一回事。
- **最深的一类：一步棋的真实想法，可能连引擎都读不出来。**
  最好的例子就是**样例二 · 10.O-O**：这一步看起来是"放着 a1 的车不管"，
  引擎给的分和另一手（11.Nd2… 那种先补车的走法）几乎一样；
  而它真正的想法是**弃掉 a1 的车，换 d 线上的攻势**（黑方真去吃车，白方 `dxc6` 之后就压不住了）。
  这条线**没有任何可复算的判据**能支撑，材料和引擎都给不出这个"道理"。
  这种局面的正确做法不是硬讲，而是**闭嘴** —— 这也是整个项目最后学到的一件事。

---

## 八、当前状态

| 项目 | 数字 |
|---|---|
| 六套测试 | **1225 项断言，0 失败**（121 / 56 / 32 / 58 / 435 / 523） |
| 代码量 | 后端 4.9k 行、前端 5.5k 行（含 HTML/CSS）、**测试 6.2k 行**，另有 5 个工具脚本 0.6k 行，合计约 1.7 万行 |
| 开发日志 | 27 轮、9.6 万字符，每个 bug 都记了根因和验证方法（只保留在作者本地，未随仓库发布） |
| 一次讲棋 | 约 1.3 万字符 ≈ 5900 token（系统提示词 5921 字符 + 单子 7110 字符） |
| 一次算棋 | 开局 <1s、中局 2～3s、战术局面可能十几秒（有墙钟上限兜底） |

---

## 九、如果还要继续做

按性价比排序（前两条已经做完，留着当记录）：

1. ~~剪提示词~~（第二十一轮剪过一次：5820 → 5150 字符，方法就是合并同类项 + 靠 70 条断言兜底）。
   **但后面几轮又往里加了规矩，现在已经涨回 5921 字符** —— 所以这件事值得再做一轮。
   纪律不变：**再加规则之前先剪一轮**，规则太长本身就会稀释注意力、招来幻觉。
2. ~~两步威胁~~（已完成：`followUp`，叉子这类"将一军、他只能应、我再吃"终于有实体了）。
3. ~~多轮记忆~~（已完成：让模型在结尾多写一行 `【要点】`，前端收起来下次带上；
   只做主线，支线不串线）。
4. **还能做的**：把"多轮记忆"扩到支线；把材料里那些"两三行"的段落再合并一次；
   给界面加一个"这次讲的哪几句是我不确定的"标记。
5. **不建议做的**：兵形/弱格/计划类讲解（量化不了，硬做只会变成空话）；
   让模型自己去搜索（那就违背了整条核心原则）。

---

## 十、这个项目真正的产出

代码是次要的。真正留下来的东西是这三样：

1. **一种分工**：让不会下棋的模型讲棋，唯一的办法是把"事实"和"讲述"用代码隔开。
2. **一套查错流程**：`打印真实提示词 → 判断是没喂还是喂错 → 改材料 → 加断言`。
   27 轮里每一次"模型讲错了"，都是靠这个流程定位的。
3. **一条分寸感**：**宁可少说，不能说错**；讲不出来就留白。
   这是这个项目从第一轮到第二十七轮学到的最重要的一件事 ——
   也是"让 AI 讲棋"这件事真正难的地方：难的不是让它说，是让它**在该闭嘴的时候闭嘴**。
