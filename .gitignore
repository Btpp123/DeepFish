# ============================================================
# 不要提交的东西
#
# 这个文件的存在只有一个目的：别让密钥跟着代码跑出去。
# 一旦 key 被提交到 GitHub，几分钟内就会有爬虫扫到并拿去用，
# 而且是拿去刷你自己的账单。
# ============================================================

# ---------- 密钥 ----------
.env
.env.local
.env.*.local

# ---------- 依赖和日志 ----------
node_modules/
*.log
npm-debug.log*

# ---------- 引擎二进制（约 100MB，不该进版本库）----------
stockfish/
stockfish-*.zip

# ---------- 测试产生的临时文件 ----------
debug-out.txt
debug-err.txt

# ---------- 本机的工作台记录（不属于项目本身）----------
.workbuddy/

# ---------- 只留在本地的文档（不随仓库发布）----------
进度记录.md
